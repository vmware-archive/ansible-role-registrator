# use 512MB, type string, or 0 for unlimited
memory_limit: 0MB
docker_service_unit: /lib/systemd/system/docker.service
docker_dns_hosts:
  - "{{ inventory_hostname }}"
consul_service_uri: consul://consul.service.consul:8500